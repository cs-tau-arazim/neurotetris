class Ann:

    def __init__(self, rows, cols):
        self.weight_matrix = [[] for i in xrange(rows)]
        #for l in self.weight_matrix:


    def play(self, board):
        # TODO change
        return 'LEFT'